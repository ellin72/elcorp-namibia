[pytest]
# Add the project root to Python's import path
pythonpath = .

# Optional: make test output cleaner
addopts = --maxfail=1 --disable-warnings -q

# Optional: tell pytest where to look for tests
testpaths = tests