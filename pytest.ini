[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = --maxfail=5 --tb=short -q --strict-markers
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow-running tests
