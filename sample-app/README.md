# Sample App

Interactive CLI that walks through the full Elcorp flow: signup → KYC → payment.

## Run

```bash
cd sample-app
pip install -r requirements.txt
pip install -e ../sdks/python
python app.py
```

Make sure the backend is running on `http://localhost:5000` first.
